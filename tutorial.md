# Tutorial — `tutorial`

Built as `agentlab_tutorial.pdf`, which the README links. The same slides close
the longer talk.

| | |
|---|---|
| 1 | getting started |
| 2 | the three things it covers — `src/build_tutorial_intro.py` |
| 3 | 1 — install and run the simple example — `src/build_tutorial_install.py` |
| 4 | user and campaign directories — `src/build_tutorial_dirs.py` |
| 5 | 2(a) — run with Globus Compute, set the endpoint up — `src/build_tutorial_globus.py` |
| 6 | 2(b) — run with Globus Compute, start it — `src/build_tutorial_globus_run.py` |
| 7 | 3 — add Slack integration — `src/build_tutorial_slack.py` |

The commands on these slides are AgentLab's own. Check them against the
repository when it changes.
