# Longer talk — `agentlab_long`

No PDF is tracked for this one. Build it with `python3 build_deck.py`.

| | |
|---|---|
| 1 | title, with the full author list and logos — `build_title_alt.py` |
| 2 | background — `build_background.py` |
| 3 | what it is, and where the code is |
| 4 | how a campaign runs — `build_diagram.py` |
| 5 | the workspace — `build_workspace.py` |
| 6 | human in the loop — `build_slack.py` |
| 7 | built on the Claude Agent SDK — `build_sdk.py` |
| 8 | other features and example campaigns — `build_features.py` |
| 9 | future — `build_future.py` |
| 10 | getting started |
| 11–16 | the tutorial — see [tutorial.md](tutorial.md) |

Slides 3–7 and 10 are the lightning talk's, unchanged. The title slide is a
separate module rather than a variant of the short deck's, so the two can drift
without either breaking.
